# --- Web 框架 ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
python-multipart==0.0.19

# --- 数据库 ---
# 2.0.43+ 才修好 Python 3.13/3.14 上 Mapped 注解解析的 union 问题
sqlalchemy[asyncio]==2.0.52
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6

# --- 配置 ---
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- 采集：浏览器 ---
# patchright 是源码级打过补丁的 Playwright，消除 CDP Runtime.enable/Console.enable 泄漏。
# 与 playwright 同 API，配合 channel="chrome" 使用系统真实 Chrome。
patchright==1.49.1
playwright==1.49.1

# --- 采集：HTTP ---
# curl_cffi 提供真实 Chrome TLS/JA3 指纹，替代 aiohttp + 关闭 SSL 校验的做法。
curl-cffi==0.7.4
httpx==0.28.1
aiohttp==3.11.11
tenacity==9.0.0

# --- 队列与缓存 ---
redis==5.2.1
arq==0.26.3

# --- 调度 ---
apscheduler==3.10.4

# --- AI 与知识库 ---
openai==1.58.1
jieba==0.42.1
numpy==2.2.1

# --- 其他 ---
websockets==14.1
pillow==11.0.0
aiofiles==24.1.0

# --- 测试 ---
pytest==8.3.4
pytest-asyncio==0.25.0
